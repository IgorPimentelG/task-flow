spring:
  application:
    name: Task Flow
  data:
    mongodb:
      authentication-database: admin
      username: admin
      password: admin
      host: localhost
      port: 27017
      database: taskflow
  graphql:
    graphiql:
      enabled: true

server:
  port: 8080